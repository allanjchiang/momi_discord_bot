# Copy to .env and paste your bot token from https://discord.com/developers/applications → Bot → Token
DISCORD_TOKEN=

# Optional: your server ID (Developer Mode → right-click server → Copy Server ID).
# Slash commands then sync to that guild immediately instead of waiting up to ~1 hour for global sync.
# DISCORD_GUILD_ID=
